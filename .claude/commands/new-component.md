Crie o componente $ARGUMENTS.
- Arquivo em `src/components/$ARGUMENTS.tsx`
- TypeScript com tipo Props definido
- Usar Tailwind para estilização
- Verificar se já existe componente shadcn/ui que resolva antes de criar do zero
```

### 3. Estrutura de pastas do .claude

Depois desses passos, sua pasta `.claude` ficará assim:
```
.claude/
├── commands/
│   ├── new-page.md
│   └── new-component.md
└── settings.json    (gerado automaticamente)
```

E na raiz:
```
CLAUDE.md